<!-- radio_button_border.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <corners android:radius="1dp" />
            <stroke android:width="1dp" android:color="@color/black" /> <!-- Border color and width when checked -->
            <solid android:color="@color/white" /> <!-- Background color when checked -->
            <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="1dp" />
            <stroke android:width="1dp" android:color="@color/grey_mild" /> <!-- Border color and width when unchecked -->
            <solid android:color="@color/white" /> <!-- Background color when unchecked -->
            <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp" />
        </shape>
    </item>
</selector>
